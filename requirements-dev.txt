# Full development install — includes analysis, visualization, and test tooling.
# Usage: pip install -r requirements-dev.txt

-r requirements.txt

# Analysis & visualization (optional at runtime; required for metrics + plots)
scikit-learn>=1.3
scipy>=1.11
plotly>=5.0

# Testing
pytest>=7.0
pytest-cov>=5.0
httpx2>=0.28

# Pre-commit
pre-commit>=3.7

# Jupyter notebook widgets (enables tqdm notebook progress bars; suppresses IProgress warning)
ipywidgets>=8.0
